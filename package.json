{
  "name": "llm-wiki",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.2.15",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "test:unit": "bun test apps packages",
    "test:refactor": "bun run scripts/tests/test-refactor.ts",
    "test:linking": "bun run scripts/tests/test-linking.ts",
    "lint": "biome lint . --write --error-on-warnings",
    "check": "turbo check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "turbo": "^2.1.1",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2"
  }
}
